version: "2"
services:
  duckdns:
    image: linuxserver/duckdns
    container_name: duckdns
    environment:
      - TZ=America/Bogota
      - SUBDOMAINS=subdomain
      - TOKEN=token #Token from https://www.duckdns.org/ 
      - LOG_FILE=false #optional
    restart: always
